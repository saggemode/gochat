apiVersion: v1
kind: Service
metadata:
  name: group-svc
  namespace: gochat
  labels:
    app.kubernetes.io/name: group
    app.kubernetes.io/part-of: gochat-platform
spec:
  ports:
    - name: grpc
      port: 50055
      targetPort: 50055
    - name: health
      port: 9090
      targetPort: 9090
  selector:
    app.kubernetes.io/name: group
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: group-deployment
  namespace: gochat
  labels:
    app.kubernetes.io/name: group
    app.kubernetes.io/part-of: gochat-platform
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: group
  template:
    metadata:
      labels:
        app.kubernetes.io/name: group
        app.kubernetes.io/part-of: gochat-platform
    spec:
      containers:
        - name: group
          image: gochat/group:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: grpc
              containerPort: 50055
            - name: health
              containerPort: 9090
          env:
            - name: GRPC_PORT
              value: "50055"
            - name: HEALTH_PORT
              value: "9090"
            - name: POSTGRES_DSN
              valueFrom:
                secretKeyRef:
                  name: gochat-secrets
                  key: GROUP_DB_DSN
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /readyz
              port: 9090
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /livez
              port: 9090
            initialDelaySeconds: 10
            periodSeconds: 15
